# ============================================================
# Agentic RAG — 锁版本依赖
# 安装: pip install -r requirements.txt
# ============================================================

# LLM & Embedding
ollama>=0.6.0

# LlamaIndex Core
llama-index-core>=0.14.0
llama-index-llms-ollama>=0.10.0
llama-index-embeddings-ollama>=0.9.0
llama-index-readers-file>=0.6.0

# Qdrant (本地 editable 安装或 pip 安装)
# 如使用本地 monorepo 版本: pip install -e <path>/llama-index-vector-stores-qdrant
llama-index-vector-stores-qdrant>=0.10.0
qdrant-client>=1.18.0

# LangGraph & LangChain
langgraph>=1.1.0
langgraph-checkpoint-sqlite>=3.1.0
langchain>=1.2.0
langchain-ollama>=1.1.0

# 存储扩展层：Redis（Session / 摘要 / 热点缓存 / checkpoint）+ MySQL（持久化）
redis>=5.0            # 纯 Python
PyMySQL>=1.1          # 纯 Python，Windows 无需编译工具链（mysqlclient 要编译，跳坑）
SQLAlchemy>=2.0
# 必须锁 ~=：0.1.0 改过内部存储格式，老格式不可读，自动升级会读不出旧 checkpoint
langgraph-checkpoint-redis~=0.5.2

# Web Search
ddgs>=9.14.0

# API
fastapi>=0.135.0
uvicorn>=0.44.0

# PDF
pypdf>=6.14.0

# Pydantic
pydantic>=2.12.0
pydantic-settings>=2.13.0

# Testing
pytest>=9.0.0
fakeredis>=2.20       # 纯 Python 内存 Redis：单测不需要启动真实 Redis
